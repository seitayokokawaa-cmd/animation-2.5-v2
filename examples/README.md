# examples

Numbered example films (`.mfs.yaml`). They serve double duty: the e2e regression
corpus (every example must check clean and render in CI) and the few-shot prompts
for `mf author`. First example arrives with task M2.8.
