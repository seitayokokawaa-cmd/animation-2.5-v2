export const PACKAGE_NAME = '@motionforge/core';

/**
 * The internal clock rate in ticks per second. 120 Hz is LCM-friendly for
 * 24/30/60 fps output and aligns physics substeps (see PLAN.md §5.1).
 */
export const TICKS_PER_SECOND = 120;
