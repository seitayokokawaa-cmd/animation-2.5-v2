# assets/fonts

Bundled Noto fonts (OFL-licensed): Sans LGC, Sans Bengali, Naskh Arabic, Sans SC.
Populated from tasks M0.5 (spike) and M10.1 per `docs/plan/PLAN.md` §5.9. System
fonts are never used — identical text rendering on every machine is part of the
determinism contract.
