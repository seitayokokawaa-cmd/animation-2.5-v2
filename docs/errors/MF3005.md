# MF3005 — Voice cache missing or stale

*Family: narration.*

A narration segment or line has no frozen audio, or its text/voice changed since the last sync — timings would be wrong.

## Example

```yaml
# after editing narration text without re-syncing
```

## Fix

Run `mf voice sync <film>` to synthesize and freeze the changed segments, then re-check.
