# MF3004 — Anchor occurrence out of range

*Family: narration.*

`nth:` asks for an occurrence beyond how many times the phrase appears.

## Example

```yaml
sync:
  - { on: { phrase: the plan, nth: 5 }, do: { sfx: ding } }   # occurs twice
```

## Fix

Lower `nth:` to an existing occurrence.
